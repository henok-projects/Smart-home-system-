package com.galsie.gcs.resources.controller;


import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("language")
public class LanguageController {
    /*
    @Autowired
    LangService langService;
    @PostMapping("/syncLangFile")
    public ResponseEntity<?> downloadLangFile(@RequestBody SynchronizeLanguageRequestDTO synchronizeLanguageRequestDTO) {
       return langService.synchroniseLanguage(synchronizeLanguageRequestDTO).toResponseEntity();
    }
    */
}

